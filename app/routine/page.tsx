import { RoutineClient } from "@/components/routine/RoutineClient";

export const metadata = { title: "Mogly — Daily Routine" };

export default function RoutinePage() {
  return <RoutineClient />;
}
